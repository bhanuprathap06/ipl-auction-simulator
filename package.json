{
  "name": "ipl-auction-simulator",
  "version": "1.0.0",
  "description": "Real-time multiplayer IPL 2026 Auction Simulator",
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix client install",
    "dev": "concurrently --names SERVER,CLIENT --prefix-colors cyan,magenta \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "seed": "npm run seed --prefix server",
    "docker:up": "docker-compose up --build",
    "docker:down": "docker-compose down"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "mongodb": "^4.17.2"
  }
}
